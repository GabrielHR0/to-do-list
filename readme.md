Como rodar:

    instalar depdendencias do back:
        -npm install
            inicia back:
                -npm run dev

    instalar dependencias do front:
        -cd frontend
            -npm install
                inicia front:
                    -npm run build
                    -npm run preview

